hell.outdir = 'build'

cpp {
	input = { 'main.cpp', 'grafico.cpp', 'direto.cpp' },
	flags = '-Wall -g',
	includes = 'glib-2.0',
	links = { 'libzmq', 'sfml-all', 'yaml-cpp', 'glib-2.0' },
	multinput = true
}

exclusiveTarget ('run', command {
	command = 'build/main'
})
