hell.outdir = 'build'
cpp {
	input = { 'main.cpp', 'grafico.cpp' },
	flags = '-Wall -g',
	links = { 'libzmq', 'sfml-all' },
	multinput = true
}

exclusiveTarget ('run', command {
	command = 'build/main'
})
